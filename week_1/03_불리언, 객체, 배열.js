// 불리언(Boolean)
// true(참) / false(거짓)
let bool1 = true;
console.log(bool1);
console.log(typeof(boo1));

// undefined
// un : not, define : 정의하다
let x;

// null : 값이 존재하지 않음을 '명시적'으로 나타내는 방법
// null = undefined
let y = null;

// object(객체)
let person = {
    name: 'John',
    height : 170,
    isMarried : false,
}

// array(배열)
// 여러 개의 데이터를 순서대로 저장하는 데이터 타입
let arr = ['apple','orange','banana', 'kiwi', 'grape'];
